'use strict';

function bowling_game() {
  const roll = pins => {
    return pins;
  };
  
  const score = () => {
    return 0;
  };
  
  return {
    roll,
    score
  };
}

module.exports = bowling_game;
